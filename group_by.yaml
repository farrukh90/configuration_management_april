--- 
- name: Run tasks based on OS
  hosts: localhost 
  tasks: 
    - name: Group OS
      group_by:
        key: "{{ansible_distribution}}"


- hosts: CentOS
  remote_user: centos
  become: yes
  become_user: root
  tasks:
    - name: Install
      package: 
        name: httpd
        state: latest

- hosts: Debian
  become: yes
  become_user: root
  remote_user: admin
  tasks:
    - name: Install 
      package: 
        name: telnet
  
- hosts: EC2
  remote_user: ec2-user
  become: yes
  become_user: root
  tasks: 
    ping