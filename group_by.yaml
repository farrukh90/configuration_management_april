--- 
- name: Run tasks based on OS
  hosts: all 
  tasks: 
    - name: Group OS
      group_by: 
        key: "{{ ansible_distribution }}"
      
- hosts: CentOS
  remote_user: centos
  tasks:
    - name: Install on debian 
      package: 
        name: telnet